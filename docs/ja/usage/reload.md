# 自動更新

SkimDownは開いているフォルダ配下のMarkdown変更を監視します。

## ツリー更新

Markdownファイルが追加、削除、リネームされた場合、フォルダを再走査してサイドバーのツリーを更新します。

## プレビュー更新

表示中のMarkdownが変更された場合、プレビューを自動で再読み込みします。表示中ファイルが削除された場合は空状態に戻ります。

## 画像変更

Markdownから参照される画像の変更は、次回Markdownを再描画したときに反映されます。

